name: Frontend Deployment

on:
  workflow_dispatch:

jobs:
  deploy-frontend:
    name: Deploy Next.js Web App
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Log Placeholder Action
        run: echo "Future workflow: build Next.js applications and publish them to frontend hosting."
