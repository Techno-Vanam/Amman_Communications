name: Backend Deployment

on:
  workflow_dispatch:

jobs:
  deploy-backend:
    name: Deploy Backend API
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Log Placeholder Action
        run: echo "Future workflow: build API image, push it to ECR/registry, then deploy to server."
